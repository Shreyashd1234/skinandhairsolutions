[build]
  command = "npm run build && node scripts/prerender.mjs"
  publish = "dist/client"

# SPA fallback — all routes serve index.html, TanStack Router handles routing client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
